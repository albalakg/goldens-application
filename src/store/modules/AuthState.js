import axios from "axios";

const AuthState = {
    namespaced: true,

    state: {
    },

    getters: {
      
    },

    mutations: {
        
    },

    actions: {
        signin() {
            return new Promise((resolve, error) => {

                axios.post

            })
        }
    }
};

export default AuthState;